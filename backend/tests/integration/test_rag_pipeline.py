"""Integration test — ingest a real legal PDF, retrieve a chunk, verify citation metadata."""

import pytest


@pytest.mark.skip(reason="requires running Qdrant and MinIO")
def test_rag_pipeline_placeholder():
    pass
