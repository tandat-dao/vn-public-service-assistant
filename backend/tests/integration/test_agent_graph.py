"""Integration test — run the full agent graph end-to-end with a mocked LLM."""

import pytest


@pytest.mark.skip(reason="requires running infrastructure and LLM mock setup")
def test_agent_graph_placeholder():
    pass
